# SHL Assessment Recommender — Render API Requirements
# Lightweight — no sentence-transformers, no pandas
# Uses Gemini API for embeddings (zero RAM cost)

# Vector DB
pinecone==4.1.0

# LLM + Embeddings (Gemini API — cloud, no RAM cost)
google-generativeai==0.7.2

# API
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
httpx==0.27.0

# Utils
python-dotenv==1.0.1